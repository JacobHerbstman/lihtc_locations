# Audit LIHTC Development Linkage Review

This audit verifies that the manual two-pass decisions partition all HUD episodes as intended, preserve all protected episode values, reconcile development and site counts, leave accepted multi-episode unit totals unresolved, and keep the ten HCCI street-only sites flagged for later site review.

Run `make` from `code/`. The audit writes a concise summary and a ten-row rejected-member review table.
