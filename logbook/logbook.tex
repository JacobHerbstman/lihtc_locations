\documentclass[11pt]{article}
\usepackage[margin=0.9in]{geometry}
\usepackage[T1]{fontenc}
\usepackage{graphicx}
\usepackage[hidelinks]{hyperref}
\begin{document}
\section*{Reset to new-construction project locations}
\noindent September 11, 2026. Agent-drafted research record.

Jacob chose a simple project-location dataset: accept HUD's new-construction
classification unless repeated records or a concrete inconsistency give a reason
to investigate. The new build starts from the original HUD workbook. It uses none
of the previous physical-development links or external review decisions. The prior
workflow was removed from the active project and preserved in Git history and a
pre-reset snapshot.

\begin{center}
\small
\input{reset_summary.tex}
\end{center}

The main table keeps the earliest new-construction record at each standardized
primary address. A unique record stays even when its year or hedonics are missing.
At a repeated address, a missing year or conflicting earliest-year tie prevents
selection; identical ties can use a stable representative only when their listed
characteristics agree. All raw new-construction records, including later phases,
remain in the record table. A repeated address is a review question, not proof
that a later project is a duplicate. Addresses are not parcel identifiers.

Census address-range matches provide an independent location comparison. Matches in the reported state are compared with HUD coordinates.
Non-exact strings can pass when the locations agree; without a HUD comparison,
an exact Census match is required. A discrepancy
greater than 500 meters triggers review; that screening threshold is not an error
definition. HUD-only coordinates are retained as unconfirmed. Scattered-site
projects retain their primary point without claiming that it represents every
building. Missing units or bedroom counts do not invalidate a checked location.

The remaining work is confined to repeated-address choices and unresolved
locations. There is no requirement to reconstruct rehabilitation histories or
obtain two external sources for every project. Neighborhood characteristics and
parcel matching are outside this reset. The saved Census tract codes describe
2020 geography; the analysis has not yet chosen historical neighborhood measures.

\paragraph{Reproduction.} Run \texttt{make setup}, then \texttt{make} at the
repository root. Task Makefiles are for prepared inputs; the root declares the
full dependency graph. HUD source: pinned 2024 release, retrieved August 8, 2026.
Census: Public\_AR\_ACS2025 and Census2020\_ACS2025, requests defined September 11,
2026. The code reset follows revision \texttt{fe507fd}. Saved-file reports record
rows, keys, missingness, ranges, and fingerprints. Raw geocoder responses are
retained so ordinary builds do not send the addresses again.
\clearpage
\section*{External checks: matching totals and specific exceptions}
\noindent September 11, 2026. Agent-drafted research record; baseline
revision \texttt{36228a3}.

The original workbook exactly matches the
\href{https://www.huduser.gov/portal/Datasets/lihtc/LIHTC-2024-Tables.pdf}{HUD 2024 summary}:
55,345 records and 3,860,546 adjusted units, including rehabilitation and
territories. All ten published annual property and unit counts for 2015--2024
also match. This establishes agreement with the source release, not the accuracy
of individual records. Our 50-state-and-DC new-construction sample uses reported
unit counts and falls from 29,453 records and 1,949,494 known units to 27,708
selected records and 1,851,152 known units. Selection removes 5.9\% of records
and 5.0\% of units. New Jersey loses 17.6\% of records and 18.5\% of units;
North Carolina loses 15.2\% and 4.9\%. These losses include unresolved ties and
later records that could be additional phases.

Three exploratory records were sampled from checked locations dated 2010--2020
(R seed 20260911). Oconee Park, Georgia (GAA20100035), is coded new construction
with 117 units in HUD. However,
\href{https://www.gao.gov/assets/a204076.html}{GAO's 2010 Georgia appendix, Table 2},
describes the same 117-unit project, also called Riverview Heights, as
rehabilitation. This is concrete evidence for an exception to HUD's default
classification. Sequoia Villas, California (CAA20120846), has 18 total units in
HUD, although its bedroom counts sum to 19. California's 2012 allocation report
and the Tulare housing authority identify 19 total units; the allocation report
separately lists 18 low-income units. Its opening year still needs confirmation.
Ohio's agency and operator support Willoughbeach Terrace's 50 units, address,
and new-construction classification. These three checks do not estimate a
national error rate.

Missing construction type creates a separate coverage limitation: 1,052 of
Indiana's 1,353 source records lack it (77.8\%), versus 3.1\% in California.
River Pointe in Tell City (INA20157173) is one excluded example; Indiana
describes a new building, and its developer reports opening it in 2015. Thus,
unknown construction type cannot be equated with rehabilitation. The previous
entry's statement that only repeated addresses and locations remain was too
narrow for a claim of complete national construction coverage.

The recommendation is to retain the simple default, document the identified
exceptions, review state counts most affected by repeated addresses, and assess
missing-type coverage by state and year. No source values or selected records
were changed by this search. The external findings are recorded for correction;
the current dataset still contains the Oconee Park conflict. Recent-year totals
also remain subject to HUD reporting revisions.

\paragraph{Reproduction.} Root \texttt{make} produces the report:
\newline
\path{tasks/audits/external_benchmarks/output/checks.txt}.\newline
The task's README
records source links, sample definitions, and individual findings. External
pages were read September 11, 2026. This audit adds no production data source.
\clearpage
\section*{Automatic selection and geographic coverage}
\noindent September 12, 2026. Agent-drafted research record; previous revision
\texttt{6fbe2c2}. This entry supersedes the earlier recommendations for manual
adjudication. No individual corrections enter production.

Jacob requested state maps and tables of construction-type completeness, a
comparison of all new-construction records with first addresses, and diagnostics
of geographic exclusions. Missing or inconsistent hedonics must not remove a
location. The selected dataset therefore retains earliest-year ties even when
names or hedonics disagree: the smallest HUD ID labels the address, all tied IDs
remain recorded, and each conflicting field becomes missing. Missing years at
repeated addresses still prevent ordering. Later records are omitted by the
first-address definition, without assuming that later phases are source errors.
This change adds 488 first-address choices relative to the earlier rule.

\begin{center}\small
\input{corroboration_summary.tex}
\end{center}

The confidence sample additionally requires a usable street address, a valid
1987--2024 placed-in-service year, and a checked location for every tied first
record. Census must match the state and agree with HUD within 500 meters; an exact
Census match suffices if HUD coordinates are absent. Scattered-site and
resyndication flags exclude records. A repeated address alone is not an exclusion.
Reported total units take priority over the bedroom breakdown; inconsistent
bedroom counts become missing. Source values remain untouched. The broad,
first-address, and confidence samples have 1,949,494, 1,862,606, and 1,435,150
reported units, respectively; missing unit counts are omitted from these sums.

The first-address rule removes 4.3\% of source new-construction records. Confidence
checks remove a further 24.2\% of first-address records, with large state differences:
88.2\% in Kentucky, 43.7\% in Minnesota, and 6.4\% in Oregon. Kentucky loses 87
first-address records to unusable addresses, usually a street without a number.
Minnesota loses 222 to unusable addresses, often text indicating multiple sites.
Applying national retention rates within reported-year groups to each state's own
year mix still leaves excess losses of 68.1 percentage points in Kentucky and
20.7 in Minnesota. This descriptive adjustment does not establish representativeness.

Missing construction type affects 7,071 of 54,928 source records in the 50 states
and DC (12.9\%). Indiana's missing share is 77.8\%, compared with California's
3.1\%. The automatic exclusions are therefore geographically systematic. No
state-specific exceptions, weighting, or manual review queue is introduced.
The documented Oconee Park classification conflict remains without an ID-specific
override: passing these screens does not certify every source classification.

\clearpage
\section*{State diagnostics: two different denominators}
\noindent\includegraphics[width=0.90\textwidth]{../tasks/audits/state_diagnostics/output/type_missing_pct.png}

\noindent\includegraphics[width=0.90\textwidth]{corroboration_exclusions.png}

\small
The HTML report in \path{tasks/audits/state_diagnostics/output/diagnostics.html}
contains all-state tables, state-year coverage, unit comparisons, and mutually
exclusive exclusion reasons. The first map uses every source HUD record; the
second uses first-address records. Boundary source: Census 2024 cartographic
states, downloaded September 12, 2026 with a pinned SHA-256 checksum. HUD and
Census geocoder source bytes were not refreshed. Root \texttt{make} regenerates
these results; task reports record row counts, keys, missingness, and fingerprints.
\clearpage
\normalsize
\section*{Use HUD coordinates and make source acquisition visible}
\noindent September 12, 2026. Agent-drafted research record; previous revision
\texttt{93e5f9a}. This entry replaces the requirement for universal Census
corroboration in the preceding entry.

HUD supplies coordinates for 99 of Kentucky's 102 new-construction records.
The prior rule excluded 87 records because their addresses were unsuitable for
Census queries, even though 85 of those records already had HUD coordinates.
Jacob questioned that restriction and directed the use of HUD coordinates when
available. The dataset now uses valid HUD coordinates by default, retaining
Census comparisons as diagnostics. An exact Census address match in the reported
state supplies a fallback only when HUD coordinates are absent. A street without
a house number no longer invalidates an available HUD point.

\begin{center}\small
\input{hud_default_summary.tex}
\end{center}

The first-address sample remains 28,196 records. The final sample increases by
4,452 to 25,832 records, with 1,730,065 reported units. It contains 25,763 HUD
points and 69 Census fallback points. Missing hedonics remain permissible: 249
retained records lack total units and 6,469 lack a consistent bedroom breakdown.
The data preserve 1,310 retained HUD--Census disagreements above 500 meters;
that threshold now describes a comparison, not an exclusion. Two tied earliest
address groups have conflicting selected coordinates and remain excluded.

Kentucky increases from 12 to 94 retained records; Minnesota increases from 391
to 678. Kentucky's remaining exclusions are one missing year, two unavailable
locations, four scattered-site flags, and one resyndication flag, counted in
that priority order. Unresolved addresses with available points retain their
HUD IDs; they have not been established as distinct physical addresses. The
missing construction-type problem is unchanged, and the retained sample should
still be described as a selected sample rather than complete national coverage.

Source acquisition is now implemented by short named shell scripts. Each task
Makefile declares the actual source target, input symlink, and output producer;
only generic.make and shell\_functions.make remain as shared Make includes.
Dataset metadata reports are saved alongside their datasets through SaveData,
not represented as separate Make targets. The root retains the explicit
cross-task dependency graph. The revision follows the explicit input/output
pattern in the Gottlieb Saiz task and the hclose task discussion, without
importing older download includes or report-target machinery.

\paragraph{Reproduction.} Root \texttt{make} rebuilds the data, diagnostics, and
this logbook; \texttt{make} in paper/ checks the data and compiles the paper.
The pinned HUD workbook and archived Census responses have not changed. Existing
source snapshots are not refreshed when acquisition implementation changes;
source-vintage changes require an explicit new target and checksum.
\clearpage
\section*{State exclusions with HUD coordinates as the default}
\noindent\includegraphics[width=\textwidth]{hud_default_exclusions.png}

The state table compares the current rule with the former corroboration rule.
Kentucky's loss falls from 88.2\% to 7.8\%; Minnesota's falls from 43.7\% to 2.4\%.
These improvements result from accepting coordinates already in HUD, not from
finding new projects or introducing individual corrections. National exclusions
after first-address selection fall from 24.2\% to 8.4\%. Louisiana still loses
31.0\%, so geographic composition remains relevant even after removing the
universal-corroboration requirement.
\clearpage
\section*{Settle the methods before exploration}
\noindent September 12, 2026. Agent-drafted research record; previous revision
\texttt{3bba0c8}. Updated after Jacob clarified the intended scope.

Jacob requested settled methodological choices and readable code before viewing
further exploratory results. A separate versioned copy and Git tag were created
in error and removed after he clarified that no literal release freeze was
intended. Analysis continues to use ordinary task outputs under the agreed
first-address and HUD-coordinate rules. The dated table contains 25,832 project
locations: 25,763 HUD points and 69 exact Census fallback points. A companion
file preserves all 28,196 first-address choices, including excluded and undated
records. Missing hedonics do not exclude projects: the dated file retains 249
without total units and 6,469 without a complete consistent bedroom breakdown.
Its reported-unit sum is 1,730,065, omitting missing counts from that sum.

The 997 source new-construction records without valid HUD coordinates do not
yield roughly 427 recovered locations. That number counts addresses suitable
for submitting a query. The archived responses supply only 72 accepted exact
matches among those 997 records; 69 survive first-address and final-sample rules.
The remaining source records comprise 570 unqueryable addresses, 228 unmatched
queries, 10 tied matches, and 117 non-exact matches. HUD coordinates account for
nearly all of the final sample's location coverage.

The task documentation describes the source vintage, address normalization, date window,
earliest-year ordering, automatic tie handling, location source priority,
scattered-site and resyndication exclusions, and treatment of missing hedonics.
The variable dictionary specifies all 41 columns, including source category
codes. Reading the pinned HUD dictionary identified a labeling problem:
\texttt{N\_4BR} is described as four-bedroom units. The derived column is now
\texttt{bedrooms\_4}, replacing \texttt{bedrooms\_4plus} without changing any
values. The main R scripts now expose the transformations in numbered, linear
blocks with visible reads and writes. A comparison with the previous outputs
found identical rows, IDs, values, and ordering after that one column rename.

The methods do not resolve source coverage limitations. The dated file retains
882 records whose address identity is unresolved and 1,310 HUD--Census distance
disagreements above 500 meters. Missing construction type and other exclusions
vary across states; the known external Oconee Park classification conflict remains
documented without an individual override. This remains a selected sample,
not a complete census of construction or verified building footprints.

\paragraph{Reproduction.} Run root \texttt{make}; the selected dataset is
\path{tasks/build_lihtc/output/confident_projects.csv}. The task README and
\path{tasks/build_lihtc/codebook.md} describe the methods and variables.
The readability cleanup reproduced all existing values after the bedroom-column
rename. A fresh build using the saved sources required no network requests and
produced identical datasets. Missing actual output regenerated the same CSV and
report; missing metadata alone did not trigger a build. Removing the unnecessary
release copies and instructions changes no data or selection rule. The original
HUD and Census source snapshots remain preserved. No new exploratory plots were
introduced.
\clearpage
\section*{Missing locations and precedent in LIHTC papers}
\noindent September 12, 2026. Agent-drafted research record; baseline
\texttt{4d9bb41}.

Jacob confirmed that the remaining unlocatable projects should not become a queue
of individual Google searches and requested evidence from papers using HUD data.
All 570 new-construction source records with neither valid HUD coordinates nor
queryable addresses are already excluded from the dated location sample. The
original records remain available. The reading adds context to that decision and
changes no selection rule, source, or dataset.

The methods review covers Baum-Snow and Marion (2009), Diamond and McQuade (2019),
and Wilson, Hankinson, Magazinnik, and Sands (2024), using available author versions.
Their treatment dates, construction scope, and required geographic precision differ.
Missing locations are an acknowledged limitation, and exhaustive manual auditing
is not a universal prerequisite. These readings do not establish our exact
first-address rule; later phases can represent distinct construction. We retain
our explicit counting definition and state/year exclusion diagnostics.

The distinction between having a usable point and knowing its exact building
location remains relevant. A future analysis of very local proximity would need
to consider coordinate precision in relation to its distance bands. This does not
introduce manual adjudication into the present dataset. Missing hedonics continue
to leave otherwise usable locations in the sample.

\paragraph{Sources and reproduction.} The linked papers, sections, printed page
numbers, and access limits are recorded in
\path{tasks/audits/external_benchmarks/literature_methods.md}.
The author PDFs were converted to text in temporary storage; the unavailable
Docling dependency required the documented text-extraction fallback. Diamond and
McQuade's separate publisher-hosted appendix and replication archive could not be
downloaded; the appendix included with their author PDF was read. The record-level
check confirmed zero retained observations among the 570. Root \texttt{make}
compiles this entry; production data and the existing exploratory outputs are
unchanged.
\clearpage
\section*{One location dataset with analysis-specific sample sizes}
\noindent September 12, 2026. Agent-drafted research record; baseline
\texttt{0a79557}.

Jacob requested a feasible HUD-based definition of new construction, timing and
basic hedonics, accepting HUD coordinates and reporting changing analysis Ns.
The main dataset now contains 27,210 first-address locations with HUD coordinates.
The previously agreed first-address counting rule is retained. The separate
confidence sample and Census fallback are removed. Undated singleton locations,
scattered-site and resyndication flags, and conflicting tied HUD coordinates no
longer exclude an otherwise selected HUD point. This supersedes the sample rules
in the preceding entries, whose original exhibits are preserved.

\begin{center}\small
\input{first_address_summary.tex}
\end{center}

The main file gains 1,447 HUD-coordinate locations relative to the former 25,763
HUD points; the former 69 Census fallback points are outside the new definition.
Kentucky retains 99 locations, compared with 94 previously. The main file includes
891 missing years, 292 missing total-unit counts, 453 scattered-site flags and
110 resyndication flags. Its 1,817,049 reported units omit missing counts. HUD's
primary point does not locate every site in a scattered project, and a retained
resyndication flag does not establish fresh physical construction.

The all-HUD-ID alternative has 28,456 coordinate records; both alternatives are
compared by state and year under the same coordinate requirement. First-address
counting can omit later construction phases, while counting every HUD ID can
include repeated financing. The all-source ledger accounts for later records,
earliest-year ties, 81 rows with uncertain repeated-address ordering, and first
representatives without HUD coordinates. No manual adjudication is introduced.

The hedonics rules retain valid partial bedroom counts for 1,491 locations without
a complete consistent breakdown. Counts contradicting total units are blanked;
raw counts remain available. Targeting indicators use 1 for yes, 0 for no and
missing for not indicated. Missing characteristics do not remove locations.
Disagreements among earliest ties blank the affected characteristics.

Requiring year and total units yields 26,066 observations. Requiring a complete
bedroom breakdown as well yields 19,618; adding all three targeting indicators
leaves 7,013, including none in Hawaii or Virginia. These are illustrative control
sets, not chosen regression specifications. Reporting each analysis N is necessary;
comparison on a common sample helps distinguish changed controls from changed
observations. Coordinate gaps also vary geographically: Maine lacks HUD points
for 18.2\% of source new-construction rows, compared with 3.4\% nationally.
Missing construction type remains unchanged, so the dataset does not establish
complete national construction coverage.

\paragraph{Reproduction and verification.} Root \texttt{make} builds
\path{tasks/build_lihtc/output/projects.csv}, exclusion accounting, sample Ns,
state/year diagnostics and this logbook. A fresh GNU Make 3.81 build without
Census responses or network access reproduced the datasets, figures and HTML.
A missing output regenerated with its report; a missing report alone triggered
no work. A synthetic upstream unit change updated downstream results, retained
the location and blanked contradictory bedrooms. Source-field checks and the
paper build passed. No source bytes changed. The literature note documents
paper-specific methods; the first-address rule remains our research definition.
No neighborhood estimates or regressions were produced.
\clearpage
\section*{First address can discard later construction phases}
\noindent September 12, 2026. Agent-drafted methods reassessment; baseline
\texttt{ace2cc0}. Production data are unchanged by this review.

Jacob questioned whether retaining only the first new-construction record at an
address serves the intended dataset of new LIHTC construction, dates and basic
hedonics. The review finds a substantive reason to change that recommendation:
HUD explicitly describes separate construction phases sharing one representative
address while remaining separate project records. An address therefore does not
supply a sufficient duplicate identifier.

The first-address rule removes 1,246 of 28,456 new-construction records with HUD
coordinates. These are 617 later-year records, 550 additional earliest-year ties
and 79 records with unresolved ordering. Among the later-year records, 122 have
PHASE in their names and 522 are within five years of the first record. These
patterns support the possibility of additional phases; they do not verify every
record or its incremental units.

The source lists Weeks Field Estates Phase I at 1301 Kellum Street with 74 units
and a 2009 placed-in-service year, and Phase II at the same address with 56 units
and a 2011 year. Both have HUD coordinates and new-construction classifications.
Our rule retains the first and removes the second on address ordering alone.
This example illustrates what the rule does; no building-specific investigation
or manual correction was performed.

The recommendation is to make each HUD-reported new-construction project ID the
main observation, retaining HUD coordinates, dates and its own available hedonics.
Shared addresses and resyndication remain flags. First-address counts would be
retained as a sensitivity comparison. This better matches counting reported
construction projects; first-address counting measures the first reported event
at an address. Neither definition eliminates all source errors. The prior decision
to retain first-address selection should have been reassessed against this
research objective before describing the methods as settled.

\paragraph{Sources and reproduction.} HUD, \emph{Updating the LIHTC Database:
Projects Placed in Service Through 2006}, printed pp.~68--69, explains the
project/address distinction. The exact link and current-data queries are in
\path{tasks/audits/external_benchmarks/literature_methods.md}.
Counts use the current all-record file and the two example IDs
\texttt{AKA20090010} and \texttt{AKA20110020}. This review updates the research
record only; it does not change the main file's 27,210 observations. Root
\texttt{make} compiles this entry.
\clearpage
\section*{Count HUD new-construction project IDs}
\noindent September 12, 2026. Agent-drafted research record; baseline
\texttt{7b7ffd8}. Jacob accepted the HUD project-ID definition after the preceding review.

The main dataset now keeps every unique HUD ID classified as new construction
in the 50 states and DC with its own HUD coordinates. Shared addresses no longer
remove later records, collapse same-year records, or force characteristics to
agree. Each project retains its own cleaned fields. Missing dates and hedonics
remain permissible, and scattered-site and resyndication flags remain visible.

\begin{center}\small
\input{../tasks/build_lihtc/output/summary.tex}
\end{center}

The change restores 1,246 HUD-coordinate records: 617 later-year records, 550
additional earliest-year records and 79 records at addresses with uncertain
ordering. Both Weeks Field Estates phases now remain with their own dates and
unit counts. Removing consensus also restores total-unit counts for 190 previously
selected records. The main file includes 943 missing placed-in-service years,
114 missing total-unit counts and 1,427 valid partial bedroom breakdowns.

Reported total units sum to 1,903,603 across 28,342 known counts. The middle half
of projects has 27--82 units. Known family targeting covers 17,365 projects, of
which 63.1\% report yes; the elderly and disabled shares are 44.7\% of 13,236 and
31.5\% of 11,535, respectively. Target populations can overlap. Unknown targeting
is missing rather than no. Year, total units and a complete bedroom mix are
jointly available for 20,627 projects; adding all three targeting indicators leaves
7,384. Summary tables explicitly show each denominator.

First-address counting now runs only in state diagnostics. It yields 27,210
records and 1,834,264 reported units using each selected record's own hedonics.
The former consensus-based sum was 1,817,049; the historical exhibit is preserved.
Comparing the main and first-address series now varies counting alone. Counts,
known-unit Ns and reported units are compared for every state and year, including
separate unknown and after-release date groups. Kentucky retains 99 records.

The main dataset retains 2,287 records with repeated standardized addresses,
467 scattered-site flags and 115 resyndication flags. These remain HUD-reported
project records, not independently verified distinct physical construction.
Unit totals should not be copied across scattered sites or interpreted as a
verified housing stock. Missing construction type and geographic coordinate gaps
remain unchanged; no manual overrides or source refresh were introduced.

\paragraph{Reproduction and verification.} Root \texttt{make} builds
\path{tasks/build_lihtc/output/projects.csv}, summary tables, state/year diagnostics
and this logbook; \texttt{make} in paper/ checks the data and paper. Every main
characteristic matches its own prepared HUD record. Independent summary calculations,
a fresh GNU Make 3.81 build without Census responses or network access, missing-output
recovery and synthetic input-change propagation passed. Detailed checks are in
the state-diagnostics README. Original source bytes are unchanged.
\end{document}
